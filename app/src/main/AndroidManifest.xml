<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ilyes.sover.sapientiasoverilyes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NewAdvertisement" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutAdvertisement" />
        <activity android:name=".SplashScreen" />
        <activity android:name=".EmailPasswordActivity"/>
        <activity android:name=".SearchAdvertisement" />
        <activity android:name=".Profile" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".ListScreen" />
        <activity android:name=".GoogleSignInActivity"/>
    </application>

</manifest>